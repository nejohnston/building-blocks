import { createMuiTheme } from '@material-ui/core';

export default createMuiTheme({
    palette: {
        textColor: '#66BB6A',
        alternateTextColor: '#fff',
        primaryColor: '#66BB6A',
        accentColor: '#66BB6A'
    }
});
